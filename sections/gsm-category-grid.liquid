<section class="gsm-section gsm-categories">
  <div class="page-width">
    <div class="gsm-section__header gsm-categories__header">
      <div>
        {%- if section.settings.label != blank -%}
          <span class="gsm-section__label">{{ section.settings.label }}</span>
        {%- endif -%}
        {%- if section.settings.heading != blank -%}
          <h2 class="gsm-section__heading">{{ section.settings.heading }}</h2>
        {%- endif -%}
      </div>
      {%- if section.settings.catalog_url != blank -%}
        <a href="{{ section.settings.catalog_url }}" class="gsm-categories__catalog-link">
          {{ section.settings.catalog_label }}
          <span class="material-symbols-outlined" aria-hidden="true">arrow_forward</span>
        </a>
      {%- endif -%}
    </div>

    <div class="gsm-categories__grid">
      {%- for block in section.blocks -%}
        <a href="{{ block.settings.url }}" class="gsm-categories__tile" {{ block.shopify_attributes }}>
          <div class="gsm-categories__icon-wrap">
            <span class="material-symbols-outlined gsm-categories__icon" aria-hidden="true">{{ block.settings.icon | default: 'devices' }}</span>
          </div>
          <span class="gsm-categories__name">{{ block.settings.title }}</span>
        </a>
      {%- endfor -%}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Datansy Categorías",
  "tag": "section",
  "class": "section-gsm-categories",
  "settings": [
    { "type": "text", "id": "label", "label": "Etiqueta", "default": "Categorías Elite" },
    { "type": "text", "id": "heading", "label": "Título", "default": "Explora por Categoría" },
    { "type": "text", "id": "catalog_label", "label": "Enlace catálogo", "default": "Ver catálogo completo" },
    { "type": "url", "id": "catalog_url", "label": "URL catálogo" }
  ],
  "blocks": [
    {
      "type": "category",
      "name": "Categoría",
      "settings": [
        { "type": "text", "id": "title", "label": "Título", "default": "Categoría" },
        { "type": "text", "id": "icon", "label": "Icono Material", "default": "devices" },
        { "type": "url", "id": "url", "label": "URL colección" }
      ]
    }
  ],
  "presets": [
    {
      "name": "Datansy Categorías",
      "blocks": [
        { "type": "category", "settings": { "title": "Celulares", "icon": "smartphone", "url": "/collections/celulares" } },
        { "type": "category", "settings": { "title": "Audio", "icon": "headphones", "url": "/collections/audio" } },
        { "type": "category", "settings": { "title": "Computación", "icon": "laptop_mac", "url": "/collections/computacion" } },
        { "type": "category", "settings": { "title": "Wearables", "icon": "watch", "url": "/collections/wearables" } },
        { "type": "category", "settings": { "title": "Consolas", "icon": "sports_esports", "url": "/collections/consolas" } },
        { "type": "category", "settings": { "title": "Fotografía", "icon": "photo_camera", "url": "/collections/fotografia" } }
      ]
    }
  ]
}
{% endschema %}
