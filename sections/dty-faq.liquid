<section id="datansy-faq" class="dty-section dty-faq">
  <div class="page-width dty-faq__inner">
    <div class="dty-faq__header dty-reveal dty-reveal--from-left">
      {%- if section.settings.label != blank -%}
        <span class="dty-section__label">{{ section.settings.label }}</span>
      {%- endif -%}
      {%- if section.settings.heading != blank -%}
        <h2 class="dty-section__heading">{{ section.settings.heading }}</h2>
      {%- endif -%}
    </div>

    <div class="dty-faq__list dty-reveal dty-reveal--delay-2" role="region" aria-label="{{ section.settings.heading | default: section.settings.label | escape }}">
      {%- for block in section.blocks -%}
        <div class="dty-faq__item" data-faq-item {{ block.shopify_attributes }}>
          <button
            type="button"
            class="dty-faq__question"
            data-faq-toggle
            id="FaqQuestion-{{ block.id }}"
            aria-expanded="false"
            aria-controls="FaqAnswer-{{ block.id }}"
          >
            <span>
              <span class="dty-faq__number">{{ forloop.index | prepend: '0' | slice: -2, 2 }}</span>
              {{ block.settings.question }}
            </span>
            <span class="material-symbols-outlined dty-faq__chevron" aria-hidden="true">expand_more</span>
          </button>
          <div id="FaqAnswer-{{ block.id }}" class="dty-faq__answer" data-faq-answer role="region" aria-labelledby="FaqQuestion-{{ block.id }}" hidden>
            <div class="dty-faq__answer-inner rte">{{ block.settings.answer }}</div>
          </div>
        </div>
      {%- endfor -%}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Datansy FAQ",
  "tag": "section",
  "class": "section-dty-faq",
  "settings": [
    { "type": "text", "id": "label", "label": "Etiqueta", "default": "Preguntas Frecuentes" },
    { "type": "text", "id": "heading", "label": "Título", "default": "Resolvemos Tus Dudas" }
  ],
  "blocks": [
    {
      "type": "faq_item",
      "name": "Pregunta",
      "settings": [
        { "type": "text", "id": "question", "label": "Pregunta", "default": "¿Pregunta?" },
        { "type": "richtext", "id": "answer", "label": "Respuesta", "default": "<p>Respuesta placeholder.</p>" }
      ]
    }
  ],
  "presets": [
    {
      "name": "Datansy FAQ",
      "blocks": [
        {
          "type": "faq_item",
          "settings": {
            "question": "¿Todos los productos tienen garantía oficial?",
            "answer": "<p>Sí. Todos nuestros productos cuentan con garantía oficial del fabricante y respaldo Datansy. El plazo varía según la marca y categoría.</p>"
          }
        },
        {
          "type": "faq_item",
          "settings": {
            "question": "¿Qué medios de pago puedo usar?",
            "answer": "<p>Aceptamos tarjetas de crédito y débito (Visa, Mastercard, Amex), transferencia bancaria, PayPal, Apple Pay y Google Pay.</p>"
          }
        },
        {
          "type": "faq_item",
          "settings": {
            "question": "¿Envían a regiones fuera de Santiago?",
            "answer": "<p>Sí, enviamos a todo Chile incluyendo regiones extremas. Los tiempos de entrega varían según la ubicación.</p>"
          }
        },
        {
          "type": "faq_item",
          "settings": {
            "question": "¿Datansy tiene tienda física?",
            "answer": "<p>Operamos principalmente online con centros de distribución en Santiago. Puedes contactarnos para coordinar retiro cuando esté disponible.</p>"
          }
        },
        {
          "type": "faq_item",
          "settings": {
            "question": "¿Debo pagar el \"Aduanazo\" aparte?",
            "answer": "<p>No. Datansy gestiona toda la importación de forma legal y transparente. Los precios publicados incluyen los costos aplicables para compras en Chile.</p>"
          }
        }
      ]
    }
  ]
}
{% endschema %}
