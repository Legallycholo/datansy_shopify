<section class="dty-section page-width" style="padding: var(--space-8) 0 var(--space-12);">
  <div class="dty-section__header dty-section__header--center">
    <h1 class="dty-section__heading">{{ 'collections.all_title' | t }}</h1>
  </div>

  <div class="dty-featured-collections__grid">
    {%- for collection in collections -%}
      {%- unless collection.handle == 'frontpage' -%}
        <a href="{{ collection.url }}" class="dty-featured-collections__card dty-card">
          <div class="dty-featured-collections__media">
            {%- if collection.featured_image -%}
              {{ collection.featured_image | image_url: width: 600 | image_tag: loading: 'lazy', alt: collection.title }}
            {%- else -%}
              <div class="placeholder-image">{{ collection.title }}</div>
            {%- endif -%}
          </div>
          <div class="dty-featured-collections__info">
            <h2 class="dty-featured-collections__title">{{ collection.title }}</h2>
            <p class="dty-featured-collections__subtitle">{{ collection.products_count }} productos</p>
          </div>
        </a>
      {%- endunless -%}
    {%- endfor -%}
  </div>
</section>

{% schema %}
{
  "name": "Lista de colecciones",
  "class": "section-main-list-collections",
  "settings": []
}
{% endschema %}
